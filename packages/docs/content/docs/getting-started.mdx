---
title: Getting Started
description: Install Uzay and create your first 3D visualization
---

## Installation

Install the library using your preferred package manager:

```bash tab="npm"
npm install uzay
```

```bash tab="pnpm"
pnpm add uzay
```

```bash tab="bun"
bun add uzay
```

## HTML Setup

Uzay renders to a container element. Create an HTML file with a container div:

```html
<!DOCTYPE html>
<html>
  <head>
    <title>Uzay Demo</title>
    <style>
      #container {
        width: 100vw;
        height: 100vh;
      }
    </style>
  </head>
  <body>
    <div id="container"></div>
    <script type="module" src="./main.ts"></script>
  </body>
</html>
```

## Your First Scene

Create a `main.ts` file with a scene, a camera, and a point:

```typescript
import { Scene3D, View3D, vec3 } from "uzay";

const scene = new Scene3D();

const camera = scene.create("camera3d", {
  position: vec3(5, 5, 5),
  lookAt: vec3(0, 0, 0),
});

scene.create("axes3d", {
  x: [-5, 5],
  y: [-5, 5],
  z: [-5, 5],
});

const point = scene.create("point3d", {
  coords: vec3(2, 1, 0),
  color: "crimson",
  radius: 3,
});

const container = document.getElementById("container") as HTMLElement;
const view = new View3D(scene, camera.id, container);
```

You can orbit the camera by dragging the background, and the point is already draggable. Try it.

## Adding a Line

Connect the origin to the point. Pass `point.coords` directly as the line's endpoint, so the line tracks the point reactively:

```typescript
scene.create("line3d", {
  start: vec3(0, 0, 0),
  end: point.coords, // [!code highlight]
  color: "crimson",
});
```

Drag the point and the line follows.

## Adding a Label

Use an overlay to display the point's coordinates. The `content` is a derived atom that reads `point.coords` and formats it as text:

```typescript
import { vec2 } from "uzay";

scene.create("overlay3d", {
  position: point.coords, // [!code highlight]
  content: scene.atom((get) => { // [!code highlight]
    const { x, y, z } = get(point.coords); // [!code highlight]
    return `(${x.toFixed(1)}, ${y.toFixed(1)}, ${z.toFixed(1)})`; // [!code highlight]
  }), // [!code highlight]
  anchor: "bottom-left",
  offset: vec2(8, -8),
});
```

The label follows the point and updates its text as you drag. This is reactivity in Uzay: change an atom, and everything that depends on it updates automatically.

## A 2D Scene

Uzay also works in the plane. The 2D API mirrors the 3D one: swap `Scene3D`/`View3D` for `Scene2D`/`View2D`, use `vec2` instead of `vec3`, and create `*2d` items. Here is a graph of $y = \sin(x)$ with labeled axes:

```typescript
import { Scene2D, View2D, vec2 } from "uzay";

const scene = new Scene2D();

const camera = scene.create("camera2d", {
  center: vec2(0, 0),
  zoom: 1,
});

scene.create("axes2d", {
  x: true,
  y: true,
  tickmarks: true,
  labels: true,
});

scene.create("function2d", {
  f: (x) => Math.sin(x),
  color: "dodgerblue",
  thickness: 2,
});

const container = document.getElementById("container") as HTMLElement;
const view = new View2D(scene, camera.id, container);
```

Drag the background to pan and scroll to zoom. From here, everything you know about atoms, dragging, and overlays carries over to 2D.

## Next Steps

- [**Core Concepts**](/docs/core-concepts): Scenes, Views, and Atoms in depth
- [**3D Items**](/docs/3d-items): All available 3D items
- [**2D Items**](/docs/2d-items): All available 2D items
- [**Interactions**](/docs/core-concepts/interactions): Drag, click, and hover events

