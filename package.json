{
  "name": "uzay-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter @uzay/core dev",
    "build": "bun run --filter @uzay/core build",
    "lint": "bun run --filter '*' lint"
  }
}
