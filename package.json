{
  "name": "math-lib-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter @mathlib/core dev",
    "build": "bun run --filter @mathlib/core build",
    "lint": "bun run --filter '*' lint"
  }
}
